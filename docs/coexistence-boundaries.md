# 共存の制約とJibunKitの未対応

更新日: 2026-09-19。採用範囲の1.0基準充足・公開はユーザー承認済み。基準は、JibunKitを知って開発を始めれば幅広いSwiftアプリを機能を削らず共存させられること。サンプルの成功は接続の部分的な証拠であり、完成基準の上限ではない。
0.8.0のP0/P1と対象別の証拠・配布状態は[出荷記録](verification/2026-09-15-0.8-release.md)を参照。

## 補完責任と版ごとの対象範囲

2026-09-12の[Issue #5](https://github.com/y-aplus/JibunKit/issues/5)により、
実装優先度はP0→P1、P0完了を0.7.0、P0を維持したP1完了を0.8.0とする。
[優先実装と版の到達条件](implementation-priorities.md)を版ごとの出荷判断の正本とする。
**1.0.0の対象範囲は2026-09-15にIssue #6推奨案で確定**。P0/P1を維持し、P2-A全体とP2-B通常範囲を必須、P2-Cは既存成果で比較的低負荷に成立する範囲を採用し、P3の高度な一般化は後段とする。通常の中断・復帰・背景動作・所有者分離は省略しない。
以下の補完責任と未対応/不能の分類は維持するが、「全台帳解消が確定済みの1.0条件」とは扱わない。
後段へ送る項目を補完済みや不能に変更しない。

2026-09-19、ユーザーが1.0基準充足と公開準備を承認。同日、最終公開も承認済み。署名専用の任意機能と未観測条件は[判断記録](verification/2026-09-19-final-observation-boundary.md)に明記し、検証成功や技術的不能へ読み替えない。

## 維持する上位原則

> JibunKitへ統合した結果、独立アプリならOSやアプリ境界によって得られていた隔離・調停・所有権管理が失われるなら、その差分をJibunKitが可能な範囲で補う。

「可能な範囲」は、対象OS・署名・プロセス構成で技術的に実現できる範囲を指す。実装の複雑さ、未要求、使用例やサンプルの不在を不可能の根拠にしない。費用や作業量によって優先順位を変えても、技術的不能の判定には使わない。版の対象範囲を決める際はIssue #5の需要調査とユーザー判断に従い、対象から送った差分も未対応として残す。

対象は、JibunKitを知ったうえで開発する幅広いSwiftアプリ。既存アプリの無修正移植や、SideStoreに依存する制限の全面解除を求めるものではない。明示的な共有は許容するが、統合しただけで所有者や状態が混ざることを既定動作にしない。

## 各領域の必須評価

1. **独立時の境界**: 同じOS・署名条件で独立アプリとして動く場合、OS・アプリ・プロセス・scene・extensionのどの境界が何を担うかを特定する。独立アプリ側で既に必要な処理をOSの保証と取り違えない。
2. **統合で失う差分**: 分離されていた識別子、保存状態、イベント配送、資源要求、タスク寿命、停止/復帰などのうち、統合で何が共有・曖昧化されるかを記録する。
3. **補完する契約と実装**: 所有者の登録、取得/解放、競合時の結果、取消・中断・復帰、必要な診断を定める。Feature別のhost分岐や他Featureの内部理解を通常手順にしない。
4. **残る制約と根拠**: 補えない差分について、該当API、OS/署名条件、再現結果を示す。根拠未確認は「未調査」、実装がないものは「未対応」とする。
5. **差分を補えた証拠**: 独立に作る複数Featureの同時要求、片方の取消・終了・失敗、他方の継続、中断後の復帰を確認する。単独成功や登録APIの存在だけでは合格にしない。

## 完了と未達の判定

- 各差分の状態は「補完済み・検証済み」「補完不要（既存境界が維持される根拠あり）」「技術的に補完不能（根拠・影響・可能な緩和策あり）」「未対応」「未調査」に分ける。
- 未対応・未調査を残した領域を、複雑さや未要求を理由に合格扱いしない。技術的に補完不能とする項目も、その範囲で可能な調停や被害軽減まで省略しない。
- 各版で合意した対象の補完が実装・検証され、残存制約の根拠と開発者への影響が説明できたときに出荷を判断する。1.0は上記の合意範囲を満たして判定する。下の領域一覧は調査の入口であり、リスト外の差分を免除するものではない。
- サンプルは検証手段であり、基盤への着手条件ではない。正しく接続したFeature同士の干渉は基盤の責任として扱い、Feature固有の業務処理の誤りを修復する責任とは区別する。

## 現在の実装・検証を確認する場所

公開安定版0.8.5と未公開1.0候補の差分は[状態一覧](status.md)を参照する。
各D項目の実装・比較試験・残存制約は[統合差分台帳](coexistence-ledger.md)を正本とし、
この完成基準に古い「現在の証拠/不足」を二重管理しない。

| 確認する領域 | 台帳の対象 |
| --- | --- |
| 活動状態、実行寿命、障害、画面・共有設定 | D01〜D05 |
| 保存、復元、Keychain、通信、Web認証 | D06〜D11 |
| URL、通知、push、background、検索 | D12〜D18 |
| 同意、構成、音声、capture、Bluetooth、位置情報 | D19〜D25 |
| Widget、Intents、継続表示、SDK、外部identity、削除 | D26〜D32 |

初期棚卸しと逐次CIメモは[履歴資料](history/coexistence-initial-notes.md)へ分離した。
その中の未実装・待機表現は、現在の未対応判定には使わない。

## 外部根拠

- [AVAudioSession](https://developer.apple.com/documentation/avfaudio/avaudiosession): アプリの音声sessionはsingleton。だから調停が必要なのであり、複数Feature利用が不可能とはしない。
- [Choosing Background Strategies](https://developer.apple.com/documentation/BackgroundTasks/choosing-background-strategies-for-your-app): OSが実行機会を判断。登録とFeatureへの配送はアプリ側の責任。
- [App extensionの構造](https://developer.apple.com/library/archive/documentation/General/Conceptual/ExtensibilityPG/ExtensionOverview.html): 本体とextensionを区別し、process共有の説明を対象に合わせる。

## 文書の正本

- **補完責任・技術的判定規則**はこの文書を正本とする。
- **優先順位・版の対象範囲・合格条件**は[優先実装](implementation-priorities.md)と[plan.json](delivery/plan.json)を正本とする。
- **D01以降の差分の現在状態**は[統合差分台帳](coexistence-ledger.md)を正本とする。D番号は原則として再利用・振り直ししない。
- **2026-09-09時点の根拠付き調査スナップショット**は[統合差分調査](research/2026-09-09-integration-boundary-research.md)へ固定保存する。実装が進んでも現在状態へ書き換えず、誤りが判明した場合は訂正履歴を残す。

初期棚卸しや実装履歴は別の履歴資料へ保持し、D01以降の最新判定を二重管理しない。

## 差分台帳の記録形式

各領域・差分ごとに「独立時の保証と主体 → 統合による喪失 → 補完契約/実装 → 競合・終了・復帰の検証 → 残存制約/根拠 → 状態/残タスク」を記録する。初期棚卸し表だけではこの評価を終えたことにならない。

例: 片方のFeatureを閉じたとき、そのFeatureが所有する処理だけを停止・解放し、他方の処理を継続できることを確認する。全プロセスのクラッシュ隔離を再現できないことを理由に、所有者単位の取消まで対象外にしない。
