# 0.8.4中間版の出荷記録（公開完了）

公開VERSION0.8.4/build14、PREVIOUS0.8.3/build13。2026-09-18にmain統合・正式公開と公開IPA検査を完了。以下の準備中表記は時点別の経緯。P2-F/1.0全体の完成gate通過ではなく、対象別実機を終えた成果と明示した未確認条件を含む中間版を準備する。

## Source別の証拠

- c66b624: AR/Action/idleの代表実機と統合native69件。
- fe1a6e5: BLEの単独/両owner/片側停止・scan停止/再接続後通知の実機。
- 7e4c7409e000000f8d2cc57a963c665bdf431f71: CI35325946663、native73、共有BLE/window24、IPA検査成功。14分36秒。診断IPA SHA-256 `95336f348366604e68b7c6f18dc1dc9fbd284bdf0c6a4bae0f65c29dafe236e2`、6,418,647 bytes。取得後CRC/SHA照合済み。OS cold復元実測ではない。

## 版変更後のCI境界

通常build-ios一回にまとめ、simulator_tests=true、選択CounterのFiles復元UIを明示指定。共有/Module/要求合成、通常IPA/Widget/Share/App Intents、保存・他Feature保持を検証する。feature_validation=false、records_validation=false。雛形/Records単独の未変更範囲は41c97f1等の既存成功を再利用する。native73の成功sourceから製品差分は版番号だけであることを確認し、診断native全体は再実行しない。

通常runの準備/upload込み20–25分見込み（41c97f1は16分04秒）。小修正ごとのCIではなく、未出荷統合差分・版・通常保存回帰を一括で検証する。CI失敗時はcompile/test/取得の証拠を切り分ける。通常IPAへ戻す代表確認が必要な場合は一度にまとめる。

## 文書と公開条件

patchの影響範囲を更新し、minor全件監査と称しない。README入口、CHANGELOG/status、今回notes/ガイド/plan/検証記録のsourceと未確認を同期する。Issue #7の英語化・履歴移動・branch清掃はユーザー指示どおり1.0直前に保留し、今回実施しない。

公開前に通常IPAの全entry CRC、bundle ID/0.8.4/build14/App Group/署名、診断非混入、source/runを確認する。既存署名/保存IDとignored Zaikoを保持。公開は新tag/new assetでIPAのみ。公開後に無認証再取得とSHA/CRCを確認する。現在はまだ公開承認・検証完了を意味しない。

Bluetooth/window/Action/ARガイドは別project Sol lowスレッドのread-onlyレビューを受け、復元buffer上限、実機とSimulator/自動試験の境界、不要な手動全件試験の要求を修正した。実機のcamera解放やOS割込みを停止表示から推定しない。

## 通常版CI 35327736417の切り分け

source c0eb6b2。通常IPAのビルド・署名/metadata検査、共有ロジック、独立Feature、backup memoryは成功。Files経由の選択復元UIのみ、最初のファイル選択後にpickerが閉じず失敗した（95行）。JSONは144 bytes、enabled/hittable=true。選択前後の画像とも同じFiles一覧で、復元処理へ進んだ証拠はない。製品不具合かOSのremote picker操作の不安定さかは未確定。41c97f1成功時からBackupScreenとこの試験のコード差分はない。

次の試験は同一ファイルが表示されたままの場合に限り、5秒後にファイル名を一度押し直す。最初の未遷移を画像・ログに残し、cancel保存保持・Counter実復元・Reminder保持・再起動のassertを維持する。製品コードと版は変更しないため、成功した通常IPA/共有試験を再利用し、focused UIだけ再実行する。押し直し成功を根本原因解消とは扱わない。

## 再試験35329389758と原因の切り分け

24fe767の一回押し直しでも失敗。両失敗runのOSログを照合し、Filesのdid Pick URLs / Notifying hostの後、JibunKitプロセス内のOS FileProviderがbookmark解決に失敗（NSFileProviderErrorDomain -1005、resolver -1012）、DocumentManagerが空配列としてdelegate配送を止めていることを確認した。タップ未到達仮説は撤回。SwiftUIのload/復元実行より前の境界であり、同じタップの再試行は中止する。

次は同じruntime/device typeの新規Simulatorを作成して比較する。runner既設Simulatorの永続状態と初期化を切り分ける仮説検証であり、OS一般の不具合と断定しない。製品コードと成功済みIPAは変更せず、タップretryを撤去した同じend-to-end試験をfocused一回実行する。新規端末でも失敗する場合はFiles providerの独立最小比較へ進み、座標変更の反復はしない。

通常候補IPA（35327736417）は4,746,205 bytes、SHA-256 `474d6a43523c041926b8af5d79549541a665bed74d87a31ee0eee7cf8662dacf`。取得後全entry CRCとapp/Widget/Shareの0.8.4/build14を確認済み。未公開。


## 3失敗時点の判断（35331113988）

c5dc30eの新規Simulator作成は成功したが、Files以前のexport対象選択直後のisEnabled即時assert（53行）で失敗。元のFileProvider -1005について新規端末との比較結果は得られていない。遅いUI応答は記録されているが、選択不成立の理由は未確定。3回の失敗を同一原因として扱わない。

ここでCI再投入を停止し、ビルド・署名検査成功済みのc0eb6b2通常IPAを0.8.4確認候補として配布する。通常版への復帰・既存データ保持とJSON往復の代表実機確認を一度に依頼する。自動Files検証は未解決として残し、過去成功や実機成功で自動試験の安定化完了とはしない。製品コードをOSログだけで変更しない。実機結果が得られるまで正式0.8.4公開/main統合は保留。


## 通常候補の実機受領と最終出荷境界

2026-09-18、c0eb6b2/35327736417の0.8.4/build14通常IPAについて、ユーザーの「OK」を、直前にまとめて依頼した既存Counter/Reminder保持とCounter JSON書き出し→読み込み→復元対象表示の双方の成功として受領。今回の復元実行は依頼しておらず、新たな実復元成功とはしない。実復元/取消/再起動保持は41c97f1の既存自動試験を参照し、BackupScreen/backup実装の差分なしを確認する。

3失敗の自動Files試験は未解決のまま保持する。新規SimulatorではFiles前で止まり、OS/FileProvider根本原因は未確定。製品側のJSON読込が今回実機で成立したことと、自動試験不安定を区別する。

出荷用immutable sourceの成功runを固定するため、製品無変更・文書同期後に通常CIを一回実行する。simulator_tests=false、feature_validation=false、records_validation=false。共有/独立Feature・通常Release/metadata/署名/IPAを検証し、UI/nativeはsource別の既存証拠と今回実機結果を再利用する。通常だけの既存出荷は5–10分程度、今回は上限25分を目安に確認する。成功後にmainをfast-forward、新規tag0.8.4へ固定し、同一IPAを公開する。


## 公開完了

source/tag `0878ede3799d732a511b6c507014f030ca50e858`、CI35333186866成功（通常job5分30秒）。mainへfast-forwardし新規tag0.8.4を固定。公開IPAは4,746,203 bytes、SHA-256 `932463711a70ca97c34c3ed1b8f6213eec4b7853372a7d6c23d02ecf7dd76f54`。3bundleの0.8.4/build14/既存ID・App Group、全entry CRC、診断構成非混入を照合。正式releaseから無認証で再取得しSHA/CRC一致。追加ZIPなし。実機確認source c0eb6b2とのSources/Modules/Package/Project差分なし。Files自動UI未解決を成功に読み替えず、1.0残件も維持。公開後文書同期commitは製品無変更。
