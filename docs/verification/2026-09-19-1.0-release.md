# 1.0.0 release preparation

Status: final publication approved on 2026-09-19; pre-publication audit below supersedes the earlier preparation checklist. Stable: 0.8.5/build15. Candidate: 1.0.0/build16.

The user accepted the v1 criteria on 2026-09-19, retaining the explicitly unobserved conditions in [the acceptance record](2026-09-19-final-observation-boundary.md). Publication was separately approved later the same day.

## Candidate build boundary

Product source under Sources/, Package.swift and Project.swift is identical to physically checked normal source feab170 before the candidate version bump. Changes since 0.8.5 concern diagnostic fixtures, verification tooling and documentation. Candidate Project.swift changes only bundle versions/builds; build-ios.yml expected versions follow them.

Run normal build-ios with simulator_tests=true, feature_validation=false, records_validation=false, and the exact UI filter MigrationUITests/MigrationUITests/testMiniAppLinksOpenColdAndSwitchWarm. This checks shared/Module tests, normal app/widget/metadata/IPA and the affected normal launch route. Reuse unchanged standalone/generated Feature and Records evidence; do not describe the filtered UI run as the full UI matrix. Prior normal run35357260854 took about the usual normal boundary; budget 25 minutes expected, investigate if it exceeds30. Documentation work proceeds independently.

## Still required before final approval request

- Verify candidate source/run, bundle versions, signing structure, all IPA entries/CRC, diagnostic exclusion and SHA-256.
- Complete Issue #7 document and branch cleanup, preserving active worktrees and ignored Zaiko.
- Read all current documents, record reviewed commit and path checklist, update current statements and links. Historical evidence is retained.
- Produce final release notes and an exact approval summary. Do not publish automatically.


## Issue #7 preparation progress

Release notes moved to `docs/releases/`; the withdrawn early v1 text is preserved at `2026-09-09-withdrawn-release-notes.md`. Current candidate notes are explicitly unpublished. GitHub Releases remain immutable published artifacts; CHANGELOG summarizes user-visible changes, while file notes supply version-specific publication text. Historical tags/releases were not changed.

Remote branch cleanup checked current origin/main ancestry, attached local worktrees, branch URLs in tracked Markdown and open PRs (none). Deleted72 remote heads atomically with expected-SHA leases; their commits remain reachable from origin/main. Local branches, worktrees, tags and ignored private files were untouched.28 heads associated with local worktrees are retained pending their owners' completion; six heads with unique ancestry are retained rather than inferring squash equivalence. Main/HEAD remain protected. This is a one-time review, not a new permanent branch ledger.

Retained unique-ancestry heads:
- `chatgpt/adopt-adaptive-parallelism` (`ef0a25832532056b4b65344571c040874a316a7f`): preserve until patch-equivalence/unique-intent review.
- `codex/core-without-zaiko` (`570735369d299c2dec8087122820f06872ba3fa4`): preserve until patch-equivalence/unique-intent review.
- `codex/p1-device-acceptance-plan` (`fd7fcf3f0b94009f590c996a56b6fabcc05c0510`): preserve until patch-equivalence/unique-intent review.
- `codex/p1-device-check-notes` (`f366a53d96eef526c96a0bbb6f465f9cb0c12b28`): preserve until patch-equivalence/unique-intent review.
- `codex/shared-refresh-journal` (`2118f69103c7066763e9860a1624c5bdf08749d6`): preserve until patch-equivalence/unique-intent review.
- `codex/simplify-mini-app-integration` (`508b3fa96c167fdd6da23120944d650ab822b8a3`): preserve until patch-equivalence/unique-intent review.

Future cleanup: after integration, verify no unique changes, live worker/worktree or branch URL dependency; delete the exact reviewed remote head with an expected-SHA lease. Do not enable automatic deletion for unrelated repository operations without considering non-PR work.


## Candidate CI and downloaded IPA

[CI35409750733](https://github.com/y-aplus/JibunKit/actions/runs/35409750733), source `82553cfe2c81cd83111cd2a247c90f87bd1015ac`, succeeded. Shared443 tests (2 existing Keychain skips, zero failures), independent Module11 tests (zero failures), normal Release/metadata/IPA checks and the selected cold/warm URL test (49.368 seconds) passed. Optional jobs were not selected and their skipped status is not test success.

Downloaded normal IPA: 4,747,918 bytes; SHA-256 `6d077f2caf52f9a1151e0e00cdb02332536baaed922e977b1ac0e7afe3e84d3f`. All44 ZIP entries passed CRC. App `com.jibunkit.app`, Widget `com.jibunkit.app.Widget`, Share `com.jibunkit.app.Share` each report1.0.0/build16. This artifact is not a published release. The Actions artifact ZIP hash is different from the IPA hash and is not used as the IPA digest.

## API guide review

Worker reviewed all44 guides. The first submission only added short English summaries and was rejected as insufficient. Follow-up adds concrete API connection/order, cancellation, generation and failure/management contracts, retaining original source notes and examples. Parent spot review of lifetime/location corrected the false statement that all Simulator evidence is structural: actual cold Region OS delivery was observed. The guide commits2f6503f/51df46a are integrated; full current-document audit and link checks remain pending, so this is not a completed release audit.


The delivery gate now carries the four owner-accepted device observation limits as `approved-unverified`, using the existing explicit unverified-record mechanism. Ownership, integration and documentation criteria remain active. A final report must carry the exact scope/reason and must not present these exclusions as passed device evidence. Signed CloudKit/APNs exclusions remain separate. Other device criteria cannot acquire this exemption. This implements the user's acceptance, not a new reduction inferred by the agent.


Usage-document review: integrated2762376 after reviewing update/recovery and SideStore sections. The six documents cover network integration, runtime restoration, scene navigation, shared refresh, updating and optional SideStore installation. Update instructions preserve data/identity and distinguish code rollback from schema rollback; portable-package tests are conditional on dependencies. SideStore remains one observed path rather than a product dependency. Stable/candidate distinction is retained. Current root/entry documentation is still under separate review; do not treat this subset as the complete audit.


## Final pre-publication audit

The user explicitly authorized publication and main integration on 2026-09-19. No further approval is required. The historical pending statements above describe intermediate checkpoints.

Integrated entry submissions f212ea7/623756f after checking the actual template API, package dependencies and workflow inputs. Parent corrected `NotesRootView()` and main-actor definition, made integration source inclusion explicit, restored the package-connection diagnostic and repaired its old anchor. English current guides, entry/use documents, compatibility and security are primary; Japanese detailed reference and historical/maintainer evidence remain supplementary. This does not translate old issue/device records or claim all internal prose is English.

Review coverage: all44 API guides, six usage documents,28 internal plan/delivery documents were read by the assigned workers; parent reviewed their submitted changes, corrected current-state statements and spot-checked lifetime/location/capture/action/widget contracts. Parent read the four entry documents, compatibility, status, releasing, priorities, coexistence boundaries/ledger, CHANGELOG, SECURITY, THIRD_PARTY_NOTICES and Records README. Reasons are grouped here; the final JSON contains the full dynamic path checklist and reviewed commit rather than duplicate per-file hashes. Changed documents clarify ownership, environment limits, release state and source-specific evidence; unchanged contracts retain their verified meaning. No undocumented current release blocker remains; six explicitly unverified scopes remain recorded, not passed.

The final report consolidates78 active criteria and six approved unverified scopes. Earlier wave failures/costs and source-specific observations remain in original reports. The single new release CI is35409750733, not a claim that all implementation used one CI. The generated Notes/host/upgrade/resource proof is retained from baa041f/34705653297 through the source-specific0.8 evidence chain. Template/package products and Records implementation are unchanged; only its README changed. Current standalone Counter and backup harness were rebuilt. Normal product Sources/Package are unchanged from feab170; Project differs only in version/build. The final tag's later changes are documentation and delivery tooling, so the IPA stays pinned to82553cf.

Candidate log confirms app/Widget/Share IDs, native metadata, en/ja InfoPlist resources, App Group and ad-hoc signature/deep strict verification. Downloaded IPA checks confirm44 entries, CRC, all three versions, expected executables and no diagnostic owner IDs. Local relative Markdown targets pass; the existing Japanese punctuation anchor in releasing is valid under GitHub slug rules. The obsolete package-connection anchor was fixed.22 delivery-validator tests pass. Tracked publication material and final product diff are checked before the tag.

The old1.0 draft contains a September9 candidate IPA and has no remote1.0.0 tag. Its unpublished asset will be replaced with the current verified IPA. No published release or existing tag is replaced.72 proven-ended remote heads were cleaned;28 attached worktree heads and six unique-ancestry heads remain intentionally preserved. No automatic branch-deletion setting or original dirty worktree is changed.
