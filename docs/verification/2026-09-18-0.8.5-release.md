# 0.8.5出荷照合

2026-09-19、ユーザーから代表手順「1–2 OK」を受領。通常IPA上書き後のCounter/Reminder保持と、Counter表示からホームへ移動・再入場した際の表示/操作が成功。cold復元の追加実機成功とはしない。確認済みfeab170のIPAを変更せず0.8.5へ使用する。以下の候補準備・未実施表記は当時の履歴。

正式公開完了。tag `0.8.5` は293248db8cf2d5fd19e1b5c6191df232e33526e2、mainへ同commitをfast-forward pushした。build source feab170以後の差分は文書だけで、IPAを再生成していない。releaseはdraft=false/prerelease=false、公開URLからの無認証再取得SHA-256は下記と一致。元workspaceの未commit変更（CONTRIBUTING.md、Modules/Records/README.md）とignored個人Featureは維持した。

候補VERSIONは0.8.5/build15、PREVIOUSは公開済み0.8.4/build14。候補sourceはfeab170、通常CI35357260854を投入。完了通知をScheduled Taskへ登録した。main統合・tag・正式公開は未実施。

## 差分と証拠

0878ede（0.8.4）からの製品変更はscene別選択IDの保存/復元/無効保存値の消去とCoreBluetooth復元の診断ログ追加。Modulesには差分なし。版は本体/Widget/Share/任意Actionとworkflow期待値を0.8.5/15へ同期した。既存データIDは変更しない。

- 99632e3 / CI35355170934: iPad(A16)/iOS26.5 Simulator、native82件・OS UI5件、失敗0/skip0。終了後の二session・別owner/値復元と片側破棄後他方保持、無効保存先消去、背景位置callback、英語Widget/camera許可文言を確認。全体15分52秒。
- 同runの診断IPAは6,483,398 bytes、SHA-256 `4b32340ce0056fa203106ee46c8169196e29a678603e06e079f0ac59329b4722`。取得後の全entry CRC検査成功、全4bundleは0.8.4/build14。この診断IPAを0.8.5通常IPAとして配布しない。
- BLE実機は59f7081の新process/同一復元世代Notify記録と、それ以前の通常通信/両owner/片側停止後保持を参照。OSの起動契機は未判定。
- 通常CI35357260854は共有/Module・通常IPAと`testMiniAppLinksOpenColdAndSwitchWarm`を一括検証する。後者は通常リマインダーが終了後に復元する前提条件を確認してから、cold counter URLがそれに優先するかを確認する。限定UIを全UI回帰と呼ばない。

未変更の生成Feature/Recordsは0.8.4時点のsource別証拠を参照。Files自動UIは独立UIKit/SwiftUIにも再現するFileProvider失敗のため同じ操作を反復しない。過去実機のJSON成功を今回IPAの実機成功とは記載しない。ローカル35試験、限定UI識別子検査、diff検査成功。

## patch文書レビュー

feab170で以下の変更範囲を確認した。minorの全件レビューではない。

- `CHANGELOG.md`、`release-notes-0.8.5.md`、`docs/status.md`、`docs/releasing.md`: 公開0.8.4と未公開候補0.8.5を区別。
- `docs/delivery/plan.json`、`docs/guides/bluetooth-ownership.md`、`docs/guides/window-scene-ownership.md`、`docs/verification/2026-09-18-ar-followup.md`: 完了したBLE採用範囲、Simulator復元成功、物理iPad等の未確認を同期。
- `README.md`: 公開0.8.4案内はまだ正しいため保持。公開時に0.8.5へ更新する。
- `release-notes-0.8.4.md`と過去出荷記録: 当時の証拠として保持。

追跡ファイルにp12/mobileprovision/ipa/xcarchive/pem/key拡張子の出荷材料なし。元workspaceの変更とignored Zaikoは変更しない。通常IPAの診断混入・bundle/版/署名・CRC・取得SHA照合は通常CI後に実施する。

## 残る出荷処理

通常CI結果とIPAを照合した後、必要な通常版の代表確認をまとめて案内する。iPad準備やBLE一式の再試験は要求しない。実機確認は通常版への上書きによるデータ保持と今回の選択復元を対象に限定する。実機報告・通常IPAサイズ/SHA・最終source・公開結果は未記入であり、公開済みとは扱わない。

## 通常候補のCIと配布

feab1702777c4f49cde4145faab93fe9dbfb4747 / CI35357260854は成功。共有443件（既存Keychain skip2）、Module11件、通常版build/IPAと限定UI `testMiniAppLinksOpenColdAndSwitchWarm`（36.987秒）が成功。通常リマインダーの終了後復元を確認してからcold counter URLで切替できた。

IPAは4,747,914 bytes、SHA-256 `f1d1cb0ff56721f5add340a629fd4f86536a28b986ebada08a5394c5767dff42`。取得後CRC成功、本体/Widget/Shareの3bundleが0.8.5/build15、App Group維持、診断owner ID非混入を確認。署名構造はCIのcodesign検証が成功。候補tag `v0.8.5-check-20260918` をsource固定で新規作成しpre-releaseとして配布。正式0.8.5/main統合は未実施。

無認証の公開URLからIPAを再取得し上記SHA-256と一致。実機依頼は通常候補への上書き後のCounter/Reminder保持と、Counter選択→ホーム→再入場で表示/操作が継続できる代表確認に限定する。後者は前景復帰確認でありcold復元の追加実機証拠とはしない。cold復元は今回の自動試験を参照し、強制終了/再起動の反復は依頼しない。報告まで正式公開を保留する。
