# AR / Action統合チェックポイント（出荷判定ではない）

2026-09-18。実装基点3a00370。P2-FのうちP2-12/P2-13を先行統合した開発検証であり、P2-S実機、P2-B/P2-I実OS、P2-6採否等の未完を通過扱いにしない。P2-F全体のpreflight/ci/release gateを合格したとは記載しない。main/公開版は0.8.3のまま。

## 投入前のレビューとローカル証拠

ARの開始元scene束縛、世代固定forwarderとcallback入口snapshot、adapter解放後も保持するpause/stream cleanup、Feature delegate保持をレビュー。ActionはShareの既存controllerを共通化し、入力受付/receipt/取消joinを維持。通常targetはAction nil、診断のみopt-in。別projectスレッド2本の提出を親で統合し、各workerはCI未実行。Swift6/APIアクセスの静的照合は実Xcode compileとは区別する。

統合Python試験102件成功。既存2件のgenerator期待値を、現在のMiniAppWindowOwnership経由の管理接続へ更新。AR generator fixtureのAction宣言二重追加も修正。plan構造検査25単位/12境界が成功。WindowsではSwift/Xcode未実行。

## この一括境界のCI

同じcommitで次の2 runを投入する。AR/Actionの初回境界は最大3回を目安に再切り分けし、workerごとに回さない。

- 通常 `build-ios.yml`: simulator_tests=true、ui_test_filter=MigrationUITests/MigrationUITests/testBackupRoundTripRestoresOnlySelectedCounter。shared全体・Records・通常IPA/metadata・standalone Counter・backup harnessと選択復元UI。Capture scene変更とShare共通化があるため古いshared/IPAを全面流用しない。準備/upload込み22〜25分見込み、shared段階10分上限。
- `native-surface.yml`: surface=ar-action、ios_major=26。AR native3 + iOS限定AR adapter4 + Action2を明示収録し、xcresult全件pass/skip0を照合。診断Release IPA、Share/Actionそれぞれのextension point/principal/identity/version/App Group/arm64/signature、IPA CRCを確認。準備/upload込み15〜20分見込み。Simulatorではtracking成功を主張しない。

Files UIは35295502996と35296983783で選択後の画面が不変。後者はfilename tapでも失敗した。icon tapは未検証の仮説で、次runではenabled/hittable/frameとDEBUG BackupImportログ（presentation、completion、load）を収集し、アプリcallback到達前後を判別する。画面が同じという証拠だけでアプリ側の問題を否定しない。復元取消、実復元、再起動、Reminder保持の判定は維持した。この再検証は既存P2-SのFiles残件を含み、そこで消費した6 runをリセットしない。

## 残る実機

自動検証後の候補で、ARの実tracking/開始元離脱/停止と他owner、Actionの実OS入口/代表入力/取消/選択owner配送をまとめる。provider copy競合等は自動試験へ回し、利用者に細かいタイミング操作を要求しない。今回のnative試験だけでP2-12/P2-13 completeにはしない。

## 結果

対象sourceは`41c97f1`。通常[35298791125](https://github.com/y-aplus/JibunKit/actions/runs/35298791125)、AR/Action[35298792032](https://github.com/y-aplus/JibunKit/actions/runs/35298792032)を投入し、両runの独立した完了通知処理を登録済み。両runは成功。sourceと証拠の照合結果を以下へ記録する。

## 初回CIと独立に準備した次候補

初回source41c97f1を変更せず、完了待ち中にP2-6の採否を調査して通常範囲を採用。通常Widgetのen/ja標準resource、実built appex読戻し試験、実connected scene上の外観A/Bとsheet/再訪、idle通常Definition接続のfixtureを統合中。AR/Action生成hostへまとめ、次の必要な検証で同時確認する。外観試験は固定sleepや未接続windowによる判定を避け、目的状態のbounded waitとwindow cleanupを使用。用途説明は実built appのen/jaとWidgetへの非混入を検査する。これらは41c97f1のCI結果には含まれない。

## 35298792032の結果確認

source `41c97f1944c184540c6e297b8e6cf2dfbc5ac52d`、iOS 26.5 Simulator/iPhone 17、AR/Action native9件、failure0/skip0。対象sourceのAR3/Action2/AR adapter4に一致する構造化xcresultを確認。実trackingは未実施。setup/upload込み8分41秒。通常回帰35298791125とは別run。

診断Release IPAのShare/Action双方のnative executable/metadata/version/App Group/署名とhost deep signature検査成功。取得したIPAのCRCとSHA-256 `298830064bfc99d8b967de5baea0d610848da1b55f9584a9e9aaff01b0da14fc`をローカル再照合。実機配布/OS Action起動はまだ依頼していない。

後続候補941c24cにはP2-6外観5試験とbuilt Widget翻訳1試験、用途説明localization、同時生成接続を追加。ローカル全103件成功、用途説明の4 generatorを統合後の対象18件も成功。これらのnative実行は初回成功に含めず、通常回帰の結果を踏まえて次の一括CIへまとめる。

## 通常回帰35298791125と次の検証境界

初回と同じ41c97f1で通常回帰も成功。共有443件（既存Keychain skip2、failure0）、Records11件、通常IPA/metadata、独立Counter/backup harness build、選択CounterのFiles往復UIが成功。UIは163.097秒。JSON rowはenabled=true/hittable=true、icon位置操作で2回とも復元UIへ進み、取消後の値保持、実復元、再起動後Counter/Reminder保持まで通過した。過去のlabel tapが無視された内部理由までは確定しないが、今回の通常取込み/復元経路の成功は観測済み。

次回は追加分をまとめた`native-surface.yml surface=ar-action ios_major=26`だけを実施する。AR3/adapter4/Action2の回帰に外観5/Widget翻訳1を加え、合計15 native試験と実built locale用途説明/Widget非混入、診断IPAを照合する。追加後のCoreとバックアップ処理は41c97f1から不変で、443共有/Records/Files UIの成功を再利用。通常Widgetのresource追加とlookup変更はこの実Widget buildとbuilt appex試験で検査し、実Widget描画を代用しない。通常配布版への復帰は出荷候補時に別途確認する。準備/upload込み15〜20分見込み（初回8分41秒）、worker単位のrunなし。このAR/Action一括境界では2回目のnative投入。P2-F全体の出荷gateではない。

## 35299987230の失敗切り分け

f0c2743の追加候補は、Swift 6.3.3がP2AppearanceProbe.swiftのIR生成でabortし、試験/Release IPA未到達。通常のassert失敗や製品runtime crashではない。バックトレースの対象はBool引数のactor関数変換thunk（`$sSbScA_pSgIeAghyg_SbIeAghn_TR`）。診断ViewのBinding setterに渡したMainActorメソッド参照を明示closureへ変更する。同様のnative test handler参照も明示closureに揃えた。これがコンパイラabortを回避するかは次CIで確認し、未検証で原因確定とはしない。

## 統合診断IPAへまとめる次の一括境界

既存B/I/SとAR/Action/外観/翻訳/受信A/Bを、1つの原子的generatorで通常hostへ接続。generator連続実行は禁止し、全source/anchor/destinationを事前検証して一回だけ変更する。テストfixtureのローカル絶対path fallbackは親統合時に除去し、このcheckout内の実sourceだけを使う。

`native-surface.yml surface=p2-combined ios_major=26`をiPad Simulatorで実行予定。単一native schemeで69件（背景19、位置9、全体起動1、identity/push12、BLE/window13、AR7、Action2、外観5、Widget翻訳1）。生成host全19 ID、起動登録/通知登録/受信catalogエラーなしを実app processで確認。独立したfake試験が成功したことだけで結合後の起動を合格にしない。1回のRelease buildで5 background modes/5 BG識別子、用途説明en/ja、multi-scene、Share/Action/Widget署名、IPA CRC/SHAを検査する。CloudKit/APNs entitlementは偽装しない。

時間見込みはsetup/upload込み20〜25分。元のfamily単位の全buildを再実行する方式ではなく、単一compile/testとReleaseでまとめる。既存通常443/Records/Files成功は差分不変につき再利用。AR/Action checkpointからの累計は今回で4 run（通常1・native3）となるため、初期3回目安を超える例外を記録する。理由は外観fixtureのcompiler abort修正と、実機1候補化に必要な実結合検証を同じrunへまとめること。失敗数/費用を別surface名に変更してリセットしない。次に失敗した場合はcompile/runtime/OS境界を証拠から切り分けてから再投入する。

投入前に、既存3 familyの`HostNativeTests.swift`が同一Swift module内でbasename衝突する問題を検出。統合生成時だけfamily名付き一時sourceへコピーし、全native source数・basename一意・原本との内容一致をPythonで検査する。原本や期待test一覧は変更しない。全ローカル106件が成功し、この追加調整の対象8件も成功。

## 統合CI35300688982の確定結果

source `c66b624419b23470881a926d197118293d9bc1b2`、iPad Simulatorの69 native試験すべて成功、failure0/skip0。内訳は背景19・位置9・実host起動1・identity/push12・BLE/window13・AR7・Action2・外観5・Widget翻訳1。result.jsonとxcresult由来のsummary/個別結果を照合し、全19 Featureの登録とlaunch/host/incoming catalogエラーなしを確認した。Swift compiler abort後のclosure修正を含む候補がcompile/runtimeとも成功したが、コンパイラ内部原因を独立に確定したとはしない。

単一test/Releaseのsetup/upload込み14分37秒。5 background modes/5 BG識別子、用途説明en/jaとWidget非混入、multi-scene、Share/Action/Widgetとhost署名・IPA検査が成功。CloudKit/APNs entitlementは付与していない。通常443件/Records11件/Files回帰は41c97f1の成功を差分照合して再利用し、このrunで再実行したとはしない。

[実機用prerelease](https://github.com/y-aplus/JibunKit/releases/tag/p2-combined-check-20260918)へ診断IPAと41c97f1の通常IPAを公開。双方とも公開URLの無認証再取得、SHAと全entry CRCが一致。[配布SHAと一括手順](2026-09-18-p2-combined-device.md)。正式版番号は進めず、mainへ未統合。実機結果と条件付き実通信の判定を残すためP2-F全体やP2-6/12/13をcompleteにしない。
